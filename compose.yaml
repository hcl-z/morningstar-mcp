services:
  morningstar-cn-mcp:
    image: ghcr.io/hcl-z/morningstar-mcp:latest
    pull_policy: always
    environment:
      MCP_HOST: 0.0.0.0
      MCP_PORT: "3845"
    ports:
      - "127.0.0.1:${MCP_PORT:-3845}:3845"
    restart: unless-stopped
